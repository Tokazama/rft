#include <Rcpp.h>
#include <vector>


Rcpp::NumericVector shiftArray( Rcpp::NumericVector a, Rcpp::NumericInteger shiftx, Rcpp::NumericInteger shifty, Rcpp::NumericInteger shiftz) {
  
  // convert to stl
  vector<double> myarray
  
  int dimx = myarray[0].size()
  
  for (int i=0; i<dimx; i++)
  
  
  
  int dimy = myarray[1].size()
  int dimz = myarray[2].size()
  
  
  
  myarray[0][1][2].push_back(1);
  
  stl::iterator itx
  stl::iterator ity
  stl::iterator itz
  
  // convert to R
  
  return ;
  
}
