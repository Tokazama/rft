for i in $(find ~/Desktop/oasis/*/RAW/ -type f -name "*.hdr")
do 
sub=$(dirname $i)
id=$(dirname $sub)
echo $id
dcm2nii -a n -g n -r n -x n -o id/nifti/*.nii $i
done

for i in $(find ~/Desktop/oasis/*/RAW/ -type f -name "*.nii")
do
ii=$(basename $i)
scan=${ii%.*}
sub=$(dirname $i)
id=$(dirname $sub)
mkdir $id/nifti/
echo $ii
cp $i $id/nifti/$ii
done

echo $scan
done

antsBrainExtraction.sh -d 3 -a $i
